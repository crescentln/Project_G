name: Main Gate

on:
  workflow_run:
    workflows:
      - Ruleset Tests
      - Repository Governance
      - Secret Scan
      - CodeQL
      - Dependency Review
    types:
      - completed

permissions:
  actions: read
  checks: read
  contents: read
  pull-requests: read
  statuses: write

concurrency:
  group: main-gate-${{ github.event.workflow_run.head_sha }}
  cancel-in-progress: true

jobs:
  gate:
    if: github.event.workflow_run.event == 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      REPOSITORY: ${{ github.repository }}
    steps:
      - name: Authorize an exact pull-request head after all checks pass
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          import os
          import re
          import subprocess
          import time

          repository = os.environ["REPOSITORY"]
          with open(os.environ["GITHUB_EVENT_PATH"], encoding="utf-8") as handle:
              event = json.load(handle)
          workflow_run = event["workflow_run"]
          head_sha = str(workflow_run.get("head_sha", "")).lower()
          if not re.fullmatch(r"[0-9a-f]{40}", head_sha):
              raise SystemExit("workflow_run returned an invalid head SHA")
          if (
              workflow_run.get("event") != "pull_request"
              or (workflow_run.get("head_repository") or {}).get("full_name")
              != repository
          ):
              raise SystemExit("only same-repository pull-request runs are eligible")

          def gh_json(path: str):
              result = subprocess.run(
                  ["gh", "api", path],
                  check=True,
                  stdout=subprocess.PIPE,
                  text=True,
              )
              return json.loads(result.stdout)

          pulls = gh_json(f"repos/{repository}/commits/{head_sha}/pulls")
          eligible = [
              item
              for item in pulls
              if item.get("state") == "open"
              and item.get("base", {}).get("ref") == "main"
              and item.get("base", {}).get("repo", {}).get("full_name")
              == repository
              and item.get("head", {}).get("sha") == head_sha
          ]
          if len(eligible) != 1:
              raise SystemExit(
                  "head SHA must belong to exactly one open pull request into main"
              )

          expected = [
              ("repository-governance", 15368),
              ("full-validation", 15368),
              ("gitleaks", 15368),
              ("Analyze Python", 15368),
              ("dependency-review", 15368),
              ("CodeQL", 57789),
              ("gitleaks", 57789),
          ]
          for attempt in range(1, 7):
              check_runs = gh_json(
                  f"repos/{repository}/commits/{head_sha}/"
                  "check-runs?filter=latest&per_page=100"
              )["check_runs"]
              failures = []
              pending = []
              for name, app_id in expected:
                  matches = [
                      item
                      for item in check_runs
                      if item.get("name") == name
                      and item.get("app", {}).get("id") == app_id
                  ]
                  if not matches:
                      pending.append(f"{name}@{app_id}:missing")
                      continue
                  latest = max(
                      matches,
                      key=lambda item: (
                          str(item.get("started_at", "")),
                          int(item.get("id", 0)),
                      ),
                  )
                  if latest.get("status") != "completed":
                      pending.append(
                          f"{name}@{app_id}:{latest.get('status')}"
                      )
                  elif latest.get("conclusion") != "success":
                      failures.append(
                          f"{name}@{app_id}:{latest.get('conclusion')}"
                      )
              if failures or not pending or attempt == 6:
                  break
              print(
                  f"attempt {attempt}/6 is waiting for: "
                  + ", ".join(pending)
              )
              time.sleep(10)

          readme = gh_json(f"repos/{repository}/contents/README.md?ref=main")
          if readme.get("size") != 0 or readme.get("sha") != (
              "e69de29bb2d1d6434b8b29ae775ad8c2e48c5391"
          ):
              raise SystemExit("public root README privacy boundary drifted")

          if failures:
              state = "failure"
              description = "Required checks failed: " + ", ".join(failures)
          elif pending:
              state = "pending"
              description = "Required checks pending: " + ", ".join(pending)
          else:
              state = "success"
              description = "PR checks, CodeQL, Gitleaks, and README verified"
          description = description[:140]
          target_url = (
              f"{os.environ['GITHUB_SERVER_URL']}/{repository}/actions/runs/"
              f"{os.environ['GITHUB_RUN_ID']}"
          )
          subprocess.run(
              [
                  "gh",
                  "api",
                  "--method",
                  "POST",
                  f"repos/{repository}/statuses/{head_sha}",
                  "-f",
                  f"state={state}",
                  "-f",
                  "context=ruleset/gate",
                  "-f",
                  f"description={description}",
                  "-f",
                  f"target_url={target_url}",
              ],
              check=True,
          )
          readme_after = gh_json(
              f"repos/{repository}/contents/README.md?ref=main"
          )
          if readme_after.get("size") != 0 or readme_after.get("sha") != (
              "e69de29bb2d1d6434b8b29ae775ad8c2e48c5391"
          ):
              raise SystemExit("README boundary changed during status write")
          print(f"ruleset/gate={state} sha={head_sha}")
          if state != "success":
              raise SystemExit(f"ruleset/gate did not authorize this SHA: {state}")
          PY
